rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // For now, allow anyone to read and write to the Project collection.
    // In a production app, you would restrict write access to authenticated users.
    match /Project/{projectId} {
      allow read, write: if true;
    }
  }
}
