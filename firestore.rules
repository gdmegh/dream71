
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public read access to the projects collection
    match /projects/{projectId} {
      allow read: if true;
      allow write: if false; // We can make this more secure later
    }
  }
}
