rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public read access to projects and other collections
    // This is a temporary, permissive rule for development.
    // You should secure this before going to production.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
