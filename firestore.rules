rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public read/write access to the Project collection for testing
    match /Project/{projectId} {
      allow read, write: if true;
    }
  }
}
